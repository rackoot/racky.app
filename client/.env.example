# API Configuration
# Base URL for API requests
# In Docker environment, use the proxy (leave empty to use default '/api')
# In local development, you can specify full URL like 'http://localhost:5000/api'
# In production, set to your production API URL
VITE_API_BASE_URL=

# Client URL (for redirects, etc.)
VITE_CLIENT_URL=http://localhost:5173

# Development flags
VITE_DEV_MODE=true
VITE_DEBUG_API=false