apiVersion: v1
kind: Service
metadata:
  name: racky-app
  namespace: {{ .Values.environment }}
spec:
  externalTrafficPolicy: Cluster
  ports:
  - port: 5173
    protocol: TCP
    targetPort: 5173
  selector:
    app: racky-app
  sessionAffinity: None
  type: NodePort