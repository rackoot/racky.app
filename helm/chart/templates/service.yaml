apiVersion: v1
kind: Service
metadata:
  name: racky-app
  namespace: {{ .Values.environment }}
spec:
  externalTrafficPolicy: Cluster
  ports:
  - port: 3005
    protocol: TCP
    targetPort: 3005
  selector:
    app: racky-app
  sessionAffinity: None
  type: NodePort